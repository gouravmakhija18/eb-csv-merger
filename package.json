{
  "name": "eci_updated_records",
  "version": "1.0.0",
  "description": "Converts symmetrical data from a text file into a CSV table",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "run-purchase": "node purchase.js",
    "run-redemption": "node redemption.js",
    "generate:output": "npm run run-purchase && npm run run-redemption && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "readline": "^1.3.0"
  },
  "devDependencies": {
    "csv-parser": "^3.0.0",
    "csv-writer": "^1.6.0"
  }
}
